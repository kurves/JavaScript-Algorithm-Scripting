function insertItem(arr, num){
mid = arr.length // 2
for (let i =0; i <arr.length; i++){
    if (arr[i] < mid){
        let left = arr[i]
    }else{
        if(arr[i] >mid){
            let right = arr[i]
        }
    }
}
}